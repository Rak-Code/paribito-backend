package com.ecommerce.project.config;

// Commented out - springdoc-openapi dependency removed
/*
import io.swagger.v3.oas.annotations.OpenAPIDefinition;
import io.swagger.v3.oas.annotations.enums.SecuritySchemeType;
import io.swagger.v3.oas.annotations.info.Contact;
import io.swagger.v3.oas.annotations.info.Info;
import io.swagger.v3.oas.annotations.security.SecurityRequirement;
import io.swagger.v3.oas.annotations.security.SecurityScheme;
import io.swagger.v3.oas.annotations.servers.Server;
import org.springframework.context.annotation.Configuration;

@Configuration
@OpenAPIDefinition(
        info = @Info(
                title = "E-Commerce Product Backend API",
                version = "1.0",
                description = "REST API for E-Commerce Product Backend Application",
                contact = @Contact(
                        name = "E-Commerce Team",
                        email = "support@ecommerce.com"
                )
        ),
        servers = {
                @Server(
                        url = "https://paribito-backend-production.up.railway.app",
                        description = "Production server (Railway)"
                ),
                @Server(
                        url = "http://localhost:8080",
                        description = "Development server"
                )
        },
        security = @SecurityRequirement(name = "bearerAuth")
)
@SecurityScheme(
        name = "bearerAuth",
        type = SecuritySchemeType.HTTP,
        scheme = "bearer",
        bearerFormat = "JWT"
)
public class SwaggerConfig {
}
*/
